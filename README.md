# 🚀 فوركس كافيه - Forex Cafe

موقع تحليلات ومقالات الفوركس مع لوحة تحكم كاملة.

## 🛠️ التقنيات المستخدمة

- **Frontend:** HTML5, CSS3, Vanilla JavaScript
- **Backend:** Vercel Serverless Functions
- **Database:** Neon PostgreSQL
- **ORM:** Drizzle ORM
- **Auth:** JWT + bcrypt
- **Hosting:** Vercel

---

## 📁 هيكل المشروع

```
forex-cafe/
├── api/                    # Serverless Functions
│   ├── auth/[action].js   # Authentication API
│   ├── sections/index.js  # Sections CRUD
│   ├── articles/index.js  # Articles CRUD
│   └── settings/index.js  # Settings API
├── db/
│   ├── schema.js          # Database Schema
│   └── index.js           # Database Connection
├── js/
│   └── api-service.js     # Frontend API Client
├── scripts/
│   └── seed.js            # Database Seeder
├── index.html             # Homepage
├── login.html             # Login Page
├── admin.html             # Admin Dashboard
├── package.json
├── vercel.json            # Vercel Config
└── .env                   # Environment Variables
```

---

## ⚡ البدء السريع

### 1️⃣ إنشاء مستودع GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/USERNAME/forex-cafe.git
git push -u origin main
```

### 2️⃣ إعداد Neon Database

1. اذهب إلى [neon.tech](https://neon.tech)
2. أنشئ حساب مجاني
3. أنشئ مشروع جديد `forex-cafe`
4. انسخ Connection String

### 3️⃣ النشر على Vercel

1. اذهب إلى [vercel.com](https://vercel.com)
2. سجّل بحساب GitHub
3. Import مستودع `forex-cafe`
4. أضف Environment Variables:
   - `DATABASE_URL` = connection string من Neon
   - `JWT_SECRET` = مفتاح سري قوي
5. Deploy!

### 4️⃣ بذر البيانات الأولية

```bash
npm install
npm run db:seed
```

### 5️⃣ تشغيل محلي (للتطوير)
```bash
npm install
vercel dev
```

### 6️⃣ إنشاء حساب المدير

1. افتح `https://your-site.vercel.app/login.html`
2. اختر "حساب جديد"
3. أنشئ حسابك (أول مستخدم = مدير)
4. سيتم تحويلك تلقائياً للوحة التحكم

---

## 📊 قاعدة البيانات

### الجداول

| الجدول | الوصف |
|--------|-------|
| `users` | المستخدمين والمدراء |
| `sections` | أقسام الموقع الرئيسية |
| `subsections` | الأقسام الفرعية |
| `articles` | المقالات |
| `filters` | فلاتر البحث |
| `homepage_sections` | إعدادات الصفحة الرئيسية |
| `settings` | إعدادات الموقع |
| `media` | الوسائط المرفوعة |
| `stats` | إحصائيات الموقع |

---

## 🔐 API Endpoints

```
POST /api/auth/login    - تسجيل الدخول
POST /api/auth/register - إنشاء حساب
GET  /api/auth/me       - بيانات المستخدم الحالي

GET    /api/sections    - جميع الأقسام
POST   /api/sections    - إنشاء قسم
PUT    /api/sections    - تعديل قسم
DELETE /api/sections    - حذف قسم

GET    /api/articles    - المقالات
POST   /api/articles    - إنشاء مقال
PUT    /api/articles    - تعديل مقال
DELETE /api/articles    - حذف مقال

GET  /api/settings      - الإعدادات
POST /api/settings      - تحديث الإعدادات
```

---

## 🎛️ لوحة التحكم

### الأقسام المتاحة:

| القسم | الوظيفة |
|-------|---------|
| 📊 الرئيسية | إحصائيات وملخص عام |
| 📂 إدارة الأقسام | إضافة/تعديل/حذف الأقسام |
| 🌳 الأقسام الفرعية | هيكل الأقسام المتفرعة |
| 🏠 أقسام الصفحة الرئيسية | تخصيص مكونات الصفحة |
| 📰 المقالات | إدارة المحتوى |
| 🎚️ أزرار الفلترة | تخصيص أزرار التصفية |
| 🖼️ الوسائط | رفع وإدارة الصور |
| 👥 المستخدمين | إدارة الأعضاء |
| ⚙️ الإعدادات | إعدادات الموقع العامة |

---

## 🔐 صلاحيات المستخدمين

| الدور | الصلاحيات |
|-------|-----------|
| `admin` | كل الصلاحيات |
| `editor` | تعديل المحتوى + إدارة الأقسام |
| `author` | كتابة ونشر المقالات |
| `subscriber` | قراءة فقط |

---

## 📜 الأوامر

```bash
npm run dev        # تشغيل محلي
npm run db:seed    # بذر قاعدة البيانات
npm run db:push    # تطبيق التغييرات على DB
npm run db:generate # توليد migrations
```

---

## 🎨 تخصيص الألوان

في ملف `styles.css`:

```css
:root {
    --bg-primary: #0f1419;      /* خلفية رئيسية */
    --bg-secondary: #1a1f2e;    /* خلفية ثانوية */
    --accent: #d97706;          /* اللون الذهبي */
    --success: #10b981;         /* أخضر */
    --danger: #ef4444;          /* أحمر */
}
```

---

## 🐛 حل المشاكل

### قاعدة البيانات غير متصلة
- تأكد من صحة `DATABASE_URL` في Vercel
- تحقق من تفعيل الاتصال في Neon Dashboard

### خطأ 401 Unauthorized
- تأكد من صحة `JWT_SECRET`
- جرّب تسجيل الخروج وإعادة الدخول

### صفحة بيضاء
- افتح Console (F12) للتحقق من الأخطاء
- تأكد من نشر جميع الملفات

---

## 📞 الدعم

للمساعدة أو الاستفسارات:
- 📧 info@forex.cafe
- 💬 [Telegram](https://t.me/forexcafe)

---

## 📄 الترخيص

جميع الحقوق محفوظة © 2025 فوركس كافيه
